# 🚀 Copilot Test Site - Полнофункциональное приложение

Это полнофункциональное веб-приложение для восстановления и развития навыков в разработке. Проект содержит:

- **Бэкенд**: Node.js + Express + PostgreSQL
- **Фронтенд**: React с современным интерфейсом
- **CRUD операции**: Полный набор операций для работы с пользователями

## 📋 Структура проекта

```
copilot-test-site/
├── backend/              # Node.js + Express API
│   ├── server.js         # Основной сервер
│   ├── db.js             # Подключение к PostgreSQL
│   ├── init-db.js        # Инициализация базы данных
│   ├── package.json
│   ├── .env.example
│   └── README.md
├── frontend/             # React приложение
│   ├── src/
│   │   ├── App.js
│   │   ├── components/
│   │   │   ├── UserForm.js
│   │   │   └── UserList.js
│   │   ├── index.js
│   │   └── index.css
│   ├── public/
│   │   └── index.html
│   ├── package.json
│   └── README.md
└── README.md             # Этот файл
```

## 🔧 Требования

- **Node.js** (v14+)
- **PostgreSQL** (установлен и запущен)
- **npm** или **yarn**

## 🚀 Установка и запуск

### 1️⃣ Подготовка PostgreSQL

```bash
# Создай новую базу данных
createdb copilot_db

# Проверь подключение (опционально)
psql -U postgres -d copilot_db
```

### 2️⃣ Запуск бэкенда

```bash
cd backend

# Скопируй .env файл
cp .env.example .env

# Отредактируй .env с твоими данными PostgreSQL
# DATABASE_URL=postgresql://пользователь:пароль@localhost:5432/copilot_db

# Установи зависимости
npm install

# Инициализируй базу данных (создаст таблицы)
node init-db.js

# Запусти сервер в режиме разработки
npm run dev
```

Сервер будет доступен на: `http://localhost:5000`

### 3️⃣ Запуск фронтенда (в новом терминале)

```bash
cd frontend

# Установи зависимости
npm install

# Запусти разработческий сервер
npm start
```

Приложение откроется на: `http://localhost:3000`

## 📚 API Endpoints

### Пользователи

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/users` | Получить всех пользователей |
| GET | `/api/users/:id` | Получить пользователя по ID |
| POST | `/api/users` | Создать нового пользователя |
| PUT | `/api/users/:id` | Обновить пользователя |
| DELETE | `/api/users/:id` | Удалить пользователя |

### Проекты

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/users/:userId/projects` | Получить проекты пользователя |
| POST | `/api/projects` | Создать новый проект |

### Health Check

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/health` | Проверка статуса сервера |

## 📝 Примеры запросов

### Создать пользователя

```bash
curl -X POST http://localhost:5000/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Петров",
    "email": "ivan@example.com",
    "age": 25
  }'
```

### Получить всех пользователей

```bash
curl http://localhost:5000/api/users
```

### Обновить пользователя

```bash
curl -X PUT http://localhost:5000/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Новое имя",
    "email": "newemail@example.com",
    "age": 26
  }'
```

### Удалить пользователя

```bash
curl -X DELETE http://localhost:5000/api/users/1
```

## 🎨 Фронтенд функциональность

- ✅ Просмотр всех пользователей
- ✅ Добавление новых пользователей через форму
- ✅ Редактирование данных пользователей
- ✅ Удаление пользователей
- ✅ Красивый градиентный интерфейс
- ✅ Отзывчивый дизайн (работает на мобильных)
- ✅ Обработка ошибок

## 🛠️ Технологический стек

### Бэкенд
- **Express.js** - фреймворк для создания REST API
- **pg** - драйвер PostgreSQL для Node.js
- **CORS** - обработка кроссдоменных запросов
- **dotenv** - управление переменными окружения

### Фронтенд
- **React** - библиотека для создания UI
- **Axios** - HTTP клиент для запросов к API
- **CSS3** - современные стили с градиентами и анимацией

## 📚 Материалы для обучения

### SQL и PostgreSQL
- Изучи синтаксис CREATE TABLE, INSERT, SELECT, UPDATE, DELETE
- Поработай с внешними ключами (FOREIGN KEY)
- Изучи индексы и оптимизацию запросов

### Express.js
- Разберись с маршрутизацией (routing)
- Изучи middleware (cors, bodyParser)
- Поработай с обработкой ошибок
- Узнай про параметры пути и query параметры

### React
- Изучи useState и useEffect хуки
- Поработай с формами и валидацией
- Изучи условный рендеринг
- Поработай с обработкой ошибок в async операциях

## 🔐 Безопасность

Текущая версия не включает аутентификацию. Для production версии добавь:
- JWT токены для аутентификации
- Validation и sanitization данных
- SQL injection защиту (уже используется параметризованные запросы)
- Rate limiting
- HTTPS

## 🚨 Решение проблем

### Ошибка подключения к БД
- Проверь, запущен ли PostgreSQL: `brew services list`
- Проверь переменные в `.env` файле
- Убедись, что база данных создана: `createdb copilot_db`

### Фронтенд не может подключиться к бэкенду
- Убедись, что бэкенд запущен на порте 5000
- Проверь браузерную консоль на ошибки CORS
- Убедись, что `proxy` в `package.json` фронтенда указывает на правильный адрес

### CORS ошибки
- Проверь, что CORS включен в `server.js`: `app.use(cors())`

## 📈 Возможные улучшения

1. **Аутентификация** - добавить JWT токены
2. **Валидация** - добавить более строгую валидацию данных
3. **Кэширование** - использовать Redis для кэширования
4. **Тестирование** - написать unit и integration тесты
5. **Документация API** - использовать Swagger/OpenAPI
6. **Развёртывание** - настроить Docker и деплой на Heroku/AWS

## 📧 Контакты

Проект создан для обучения и восстановления навыков в разработке.

---

**Удачи в обучении! 🎓**
